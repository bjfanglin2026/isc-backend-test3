server:
  port: 8080

spring:
  application:
    name: 远陌云-backend
  
  datasource:
    url: jdbc:h2:file:./data/isc;MODE=MySQL;AUTO_RECONNECT=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    
  h2:
    console:
      enabled: true
      path: /h2-console
      
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

# ISC Platform Configuration
isc:
  platform:
    host: 172.16.18.85
    port: 443
    appKey: 27133966
    appSecret: aTbADLtEFsjhSHCNIXuO
    userId: openclaw03
    domainId: domain0

logging:
  level:
    com.isc: DEBUG
